> Generated by: `planner:research` skill
> Lives at: `docs/apps/{app}/system-design.md`
> Mermaid diagrams: use the `mermaid-diagram-specialist` skill

# {App Name} — System Design

## Database schema

```mermaid
erDiagram
    USER {
        string id PK
        string email
        string name
        timestamp created_at
    }
    {ENTITY} {
        string id PK
        string user_id FK
        string name
        timestamp created_at
        timestamp updated_at
    }
    USER ||--o{ {ENTITY} : owns
```

Key tables:
| Table | Purpose |
|-------|---------|
| `user` | Better Auth managed user accounts |
| `{table}` | {what it stores} |

## API surface

Base URL: `/api/v1/`
Auth: Better Auth session cookie / Bearer token

| Procedure | Method | Auth | Description |
|-----------|--------|------|-------------|
| `{router}.list` | GET | protected | {description} |
| `{router}.getById` | GET | protected | {description} |
| `{router}.create` | POST | protected | {description} |
| `{router}.update` | PATCH | protected | {description} |
| `{router}.delete` | DELETE | protected | {description} |

## Domain model (DDD)

```mermaid
graph TD
    subgraph Core Domains
        D1[{Domain 1}]
        D2[{Domain 2}]
    end
    subgraph Aggregate Domains
        D3[{Domain 3}]
        D4[{Domain 4}]
    end
    subgraph Infrastructure Domains
        D5[Files]
        D6[Email]
    end
    D3 --> D1
    D3 --> D5
    D4 --> D2
```

`packages/{app}/domains/src/` contains:
| Domain | Purpose |
|--------|---------|
| `{domain}/` | {what it manages} |

## Key packages
| Package | Role |
|---------|------|
| `@bokendell/{app}-domains` | Business logic (DDD services + repositories) |
| `@bokendell/{app}-db` | Drizzle schema + migrations + test factories |
| `@bokendell/{app}-client` | tRPC client + React Query hooks + query keys |
| `@bokendell/{app}-auth` | Better Auth configuration |

## Authentication
[How auth works — session cookies, magic link, OAuth providers, role system]

## Background jobs
[Inngest functions — list them with trigger event and purpose]

## Notable patterns
[Anything non-standard that agents need to know before touching this codebase]

## Performance considerations
[Caching strategy, expensive queries, rate limiting]
