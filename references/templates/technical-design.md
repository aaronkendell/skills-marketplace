---
linear_id:
---

> Generated by: `planner:project` skill
> Mermaid diagrams: use the `mermaid-diagram-specialist` skill

# Technical Design: {Project Name}

## Approach
[2-3 sentences describing the overall technical strategy]

## Architecture

```mermaid
graph TD
    A[{Client}] -->|tRPC| B[{API}]
    B --> C[{Service}]
    C --> D[{Repository}]
    D --> E[(Neon PostgreSQL)]
```

## Schema changes

```mermaid
erDiagram
    {TABLE_A} {
        string id PK
        string name
        timestamp created_at
    }
    {TABLE_B} {
        string id PK
        string table_a_id FK
    }
    {TABLE_A} ||--o{ {TABLE_B} : has
```

[New tables or modified columns. If none, write "No schema changes."]

## API changes
[New tRPC procedures or REST routes. If none, write "No API changes."]

| Procedure | Method | Auth | Description |
|-----------|--------|------|-------------|
| `{router}.{name}` | query/mutation | public/protected/admin | {what it does} |

## Package changes
[New packages or exports added]

## Key files to create or modify
- `{path}` — {why}

## Implementation order
1. Schema / DB migration
2. Repository
3. Service + unit tests
4. tRPC router + route tests
5. Frontend (container → hook → screen → components)
6. E2E / integration tests

## Testing strategy
[Which layers need tests, any special setup]

## Open technical questions
- [ ] {Question}
