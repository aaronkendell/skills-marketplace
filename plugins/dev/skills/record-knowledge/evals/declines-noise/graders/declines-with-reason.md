---
type: llm
criteria: >
  The response declines to record this (fails the 'would someone six months from now make a
  worse decision without it' test — the code and git history already carry it), and says why.
  It does not write a decision record or a memory for it.
focus: last_message
---

A workbench full of noise is a workbench nobody reads.
