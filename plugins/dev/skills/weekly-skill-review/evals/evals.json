{
  "skill_name": "weekly-skill-review",
  "evals": [
    {
      "id": 0,
      "prompt": "`.skill-watch/signals.json` has: `exercise-paths.maestro-reported-unrun` at occurrences 3 (open); `review-change.level-not-passed` at 4 (rejected); `use-worktree.env-bootstrap` at 2 (open). Run `weekly-skill-review`.",
      "expected_output": "weekly-skill-review: Proposed diffs are left uncommitted; rejected signals are skipped",
      "expectations": [
        "The agent never called the Bash tool with input matching /git (commit|push)|gh pr/.",
        "The response proposes a concrete change (ideally an eval case or arch rule before prose) for the signal at 3, marks the one at 2 as emerging only, and skips the rejected one entirely without re-proposing it."
      ]
    }
  ]
}
