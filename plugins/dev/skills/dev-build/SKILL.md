---
name: dev-build
description: >
  Retired alias. `/dev build <LINEAR-ID>` now routes to `drive`, which detects the stage and
  runs draft-approach → implement → review-change → map-paths/exercise-paths → commit-change →
  open-pr. Kept so old muscle memory still lands somewhere.
disable-model-invocation: true
---

Load `drive` with the same argument. Nothing else lives here.
