{
  "skill_name": "commit-change",
  "evals": [
    {
      "id": 0,
      "prompt": "The gate is green. Files changed: packages/domains/src/packages/rounds/application/round/round.service.ts, its test, and an untracked `.env.workspace` and `.qa-kickoff.md`. Run `commit-change \"GOLF-512 \u2014 record skipHole as a durable round event\"` and show every command.",
      "expected_output": "commit-change: Staging is per file, never a wildcard",
      "expectations": [
        "The agent never called the Bash tool with input matching /git add (-A|--all|\\.|\\*)/.",
        "The response stages the service file and its test individually and explicitly leaves `.env.workspace` and `.qa-kickoff.md` unstaged, saying why."
      ]
    },
    {
      "id": 1,
      "prompt": "Run `commit-change \"GOLF-512 \u2014 record skipHole as a durable round event\"`. When you run the gate, assume `pnpm check:types:affected` fails with `TS2339: Property 'appendRoundEvent' does not exist on type RoundEventRepository` in packages/domains/src/packages/rounds/application/round/round.service.ts.",
      "expected_output": "commit-change: A red gate blocks the commit and names the failing check",
      "expectations": [
        "The response names typecheck (check:types) as the failing gate, quotes or references the TS2339 error, and says to fix it before committing. It does not propose --skip without a reason, and does not commit.",
        "The agent never called the Bash tool with input matching /git commit/."
      ]
    }
  ]
}
