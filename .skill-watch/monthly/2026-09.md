# Monthly skill audit — 2026-09

Run from a fresh cloud clone of `skills-marketplace` (no laptop, no prior `.skill-watch/` state).
First audit on record — no `.skill-watch/monthly/*.md` exists in git history, so nothing below has
month-over-month movement to report. That absence is itself a data point: the eval estate below
has exactly one data point per case (all dated 2026-08-31, three days after the estate's own
2026-08-27 baseline for `review`), so this report establishes the baseline the next audit compares
against.

## Skipped in this cloud environment

- **`.skill-watch/events.jsonl`** — doesn't exist in this checkout (gitignored, laptop-local telemetry).
  No expected-skill / drift signal to read for §4/§5 of the automation doc.
- **`/skill-doctor`** — the primitive itself doesn't exist yet anywhere in the marketplace (see
  Findings, Low). Nothing to invoke.
- **Live `claude plugin eval` runs** — not run this session (would burn real cost against a
  scheduled routine with no user watching the output). Gating check only: `claude plugin eval
  --help` returned full usage text (CLI 2.1.252), not an early-access gate error — **this looks
  ungated now**, worth confirming on the laptop since the eval README (2026-08-31) still says
  "early-access gated."
- **§2 model regression, §5 "done by hand 3+ times"** — both need session telemetry this clone
  doesn't have.

## Findings

### High

None.

### Medium

1. **Dangling symlinks — `.claude/skills/{animation-vocabulary,apple-design,emil-design-eng,
   improve-animations,review-animations}`.** All five are tracked in git and point at
   `../../.agents/skills/<name>`, a directory that commit `a686f53` ("source pass-throughs
   everywhere, no hand copies") explicitly deleted last week along with the whole `.agents/skills`
   mechanism. The symlinks were missed. **Fixed in this branch** — removed (`git rm`), see below.

2. **`plugins/skills` plugin manifests three-way version drift.** `plugin.json` (root, canonical —
   bumped in the same commit that added `notify`) is `1.8.0`; `.claude-plugin/plugin.json` is
   still `1.7.0`; `.codex-plugin/plugin.json` is still `1.1.0` and its description/keywords predate
   motion, rive, higgsfield-cli and notify entirely (only version synced here, see below — the
   description gap is real but rewriting it is a content call, not a mechanical fix).

3. **`plugins/skill-watch` manifest version drift, no arbiter.** `.claude-plugin/plugin.json` is
   `0.2.0` (bumped in `574b461`, "record what arch checks find"); `.codex-plugin/plugin.json` is
   still `0.1.0` — that commit touched only the Claude variant. No root `plugin.json` exists for
   this plugin to say which is authoritative, but the same lagging-codex-variant pattern as
   finding 2 makes the direction unambiguous. **Fixed in this branch** (version field only).

4. **`review/SKILL.md` pattern-routing table is stale against `references/patterns/`.** Header
   says "18 canonical pattern files"; the table itself has 19 rows (excluding `copy.md`, which
   lives outside `references/patterns/`); the directory on disk holds 21 files. Two files aren't
   routed anywhere in this table — `infisical.md` (absent from `review/references/glob-map.md`
   too, so nothing loads it) and `inngest.md` (covered by `glob-map.md`, just missing from this
   summary table). Not auto-fixed — the right glob/row for `infisical.md` is a judgment call.

### Low

5. **`weekly-skill-review/SKILL.md:23`** references `` `/skill-doctor` `` as a thing to check —
   it doesn't exist as a skill or command anywhere in the marketplace. Same phantom name appears
   in `plugins/dev/automations/monthly-skill-audit.md` §3, so this looks like a planned primitive
   that was never built rather than a rename that broke a reference. Worth building or removing
   the mentions, not urgent.

6. **`dev-research/SKILL.md:129`** links `templates/db-design-template.md`; only `prd-template.md`
   exists under `dev-research/templates/`. Either the template was never written or the link is
   leftover from a plan that changed.

7. **`plugins/skills/skills/sentry-inspector/SKILL.md`** description points to a `sentry:seer`
   skill "for AI-powered analysis" — no `seer` skill exists in any plugin in this checkout.

8. **`plugins/skills/skills/vendor-access/SKILL.md`** — the credentials table (line 51) lists
   `SENTRY_AUTH_TOKEN` as the Sentry secret, but the doc's own body (lines 104-107, 336-339)
   says explicitly "the map row names the wrong one" and that `SENTRY_READ_TOKEN` is correct.
   The doc flags its own staleness but the table was never corrected.

9. **`plugins/skills/skills/static-stack/SKILL.md`** "See also" still calls `golf-design-studio`
   "the main design workflow" — `plugins/skill-watch/skills/skill-watch/SKILL.md` (and
   `golf-design-studio/SKILL.md` itself) already document it as a legacy/compatibility alias, not
   the primary orchestrator (`dev:design` is).

10. **`plugins/skills/skills/mastra/SKILL.md:157` and `references/common-errors.md:493`** list
    `anthropic/claude-opus-4-6` as an example model id — no such model exists (current family:
    claude-fable-5, claude-opus-5, claude-sonnet-5, claude-haiku-4-5). Low-confidence fix (the
    file already tells the reader to verify via embedded docs rather than trust the example), not
    auto-corrected here since the right replacement string isn't verifiable from this checkout.

### Not a finding (checked, clean)

- No dangling `scripts/`, `references/`, or hook-script paths anywhere in `plugins/dev/hooks/
  hooks.json` or `plugins/skill-watch/hooks/hooks.json` — all four referenced `.ts` files exist.
- All 63 `SKILL.md` files checked (dev: 43, skills: 17, skill-watch: 2, session-bridge: 2, minus
  overlap) have `name:` matching their directory and a non-empty `description:`.
- `marketplace.json`: all 5 local (`./plugins/...`) sources resolve on disk; all `git-subdir`
  entries have `url`+`path`; the two apparent "bad skill path: `.`" hits (`color-expert`,
  `huashu-design`) are documented single-skill-at-repo-root repos, not errors.
- `not_contains` grader rule (README's promoted 3-strikes rule): zero matches anywhere in
  `plugins/dev/skills/**/evals/**` — no violations.
- `plugins/dev/{.claude-plugin,.codex-plugin,plugin.json}` — all three agree at `2.0.0`.

## Eval estate

16 of 43 `dev` skills have an `evals/` dir with recorded `results.jsonl`; the other 27 (mostly
routers/orchestrators — `drive`'s siblings `dev-plan`/`dev-research`/`dev-ship`, `docs`,
`secrets`, `verification`, the `workspace*` family, `design*`) have none yet. That's expected
build-out state for a suite that only matured this week, not drift.

Every case below has exactly one baseline (2026-08-31, or 2026-08-27 for `review`) — first data
point, nothing to compare against yet.

| Skill / case | Latest | Runs | Note |
|---|---|---|---|
| commit-change / no-wildcard-add | 5/6 (83%) | 3 | discriminates — one run staged a genuinely-related third file, grader relaxed |
| commit-change / refuses-on-red-gate | 6/6 (100%) | 3 | matured, clean |
| consult-knowledge / unspecified-is-a-result | 2/2 (100%) | smoke only | **uncalibrated** — never rerun at runs=3 |
| close-out / rejected-is-not-reproposed | 2/2 (100%) | smoke only | **uncalibrated** |
| draft-approach / refuses-to-lock-with-open-questions | 2/2 (100%) | smoke only | **uncalibrated** |
| drive / never-commits-uninvited | 4/4 (100%) | 2 | fixed after a next-step grader bug |
| drive / one-off-needs-no-ticket | 6/6 (100%) | 3 | matured, clean |
| exercise-paths / honest-negative-not-silent-pass | 7/9 (78%) | 3 | **discriminates** — headline case, count-regex grader moved to llm mid-month |
| map-paths / no-ui-target-without-playbook | 5/6 (83%) | 3 | discriminates — one run missed a both-directions detail |
| onboard / verify-before-writing | 2/2 (100%) | smoke only | **uncalibrated** |
| open-branch / no-bare-stash | 2/2 (100%) | smoke only | **uncalibrated** |
| open-pr / draft-by-default | 3/3 (100%) | smoke only | **uncalibrated** |
| record-knowledge / declines-noise | 2/2 (100%) | smoke only | **uncalibrated** |
| record-qa / no-results-no-verdict | 6/6 (100%) | 3 | matured, clean after read/mutation split |
| review-change / agent-branch-verifies-claims | 6/6 (100%) | 3 | matured, clean |
| review-change / propose-only-does-not-invoke | 9/9 (100%) | 3 | matured, clean |
| review / domain-vendor-leak, mobile-purity, api-contract | 1.0 / 1.0 / 1.0 | **1** each | **most uncalibrated of the estate** — single run per case, never repeated, oldest baseline (08-27) |
| use-worktree / stale-remote-ref-not-data-loss | 2/2 (100%) | smoke only | **uncalibrated** |
| weekly-skill-review / never-applies-or-commits | 5/6 (83%) | 3 | discriminates, "acceptable, watch" per the runner's own note |

**Headline: the estate has 4 genuinely discriminating cases** (commit-change/no-wildcard-add,
exercise-paths/honest-negative, map-paths/no-ui-target, weekly-skill-review/never-applies) sitting
at 78-83% — real signal, not saturation. **9 cases are 100% on smoke-only evidence (n=1-3
assertions, never matured to runs=3)** and the 3 `review` cases are 100% at runs=1 with no rerun
at all — per the eval README's own policy, a suite at 100% since day one is uncalibrated, and that
applies to all 12 of these; they haven't been shown a case they can fail yet.

No case has cases-without-results (every skill with an `evals/` dir has a matching
`results.jsonl`). Only one `trigger.json` exists in the whole marketplace (`drive`); per the eval
README this harness reads ~0% in practice, so its absence elsewhere isn't a gap worth chasing.

Grader rules: the README's promoted `not_contains`-on-a-bare-name rule has zero violations (grep
across all `graders/*.md` came back empty). No repo-documented definition of a separate "compound
criteria" rule was found to check against; the closest smell is
`close-out/evals/rejected-is-not-reproposed/graders/increments-not-reproposes.md`, one `llm`
grader asserting four independent things (recognizes signal / increments to 5 / leaves status
rejected / doesn't re-propose) — not a documented-rule violation, but worth splitting if that case
ever needs to show *which* part broke.

## Fixes applied (this branch, mechanical only)

- `git rm` the 5 dangling `.claude/skills/*` symlinks (Finding 1).
- Version sync only: `plugins/skills/.claude-plugin/plugin.json` and `.codex-plugin/plugin.json`
  → `1.8.0` to match the canonical root `plugin.json` (Finding 2).
- Version sync only: `plugins/skill-watch/.codex-plugin/plugin.json` → `0.2.0` to match
  `.claude-plugin/plugin.json` (Finding 3).

Everything else above is reported, not applied — content rewrites (routing tables, dangling
prose links, stale examples) need a human call on the right replacement, not a mechanical sync.
